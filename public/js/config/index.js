import constants from './constants';
import translate from './translate';
import routes from './routes';
import run from './run';

export default {
  constants: constants,
  translate: translate,
  routes: routes,
  run: run
};